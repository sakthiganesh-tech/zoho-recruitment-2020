#include<stdio.h>
int main(){
int row = 5, col = 5, i, j;
for(i=1; i<=row ; i++)
{
for(j=col; j>col-i ; j--)
{
printf("%d" , j);
}
for(j=1; j<=col-i ; j++)
{
printf("%d" , (row-i + 1));
}
printf("\n");
}
return 0;
}
